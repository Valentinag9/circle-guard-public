apiVersion: apps/v1
kind: Deployment
metadata:
  name: circleguard-form-service
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: form-service
  template:
    metadata:
      labels:
        app: form-service
    spec:
      containers:
      - name: form-service
        image: circleguard-registry/circleguard-form-service:latest
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: form-service
  namespace: dev
spec:
  selector:
    app: form-service
  ports:
  - port: 80
    targetPort: 8080
