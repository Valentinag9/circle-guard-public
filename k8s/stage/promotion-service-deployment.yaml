apiVersion: apps/v1
kind: Deployment
metadata:
  name: circleguard-promotion-service
  namespace: stage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: promotion-service
  template:
    metadata:
      labels:
        app: promotion-service
    spec:
      containers:
      - name: promotion-service
        image: circleguard-registry/circleguard-promotion-service:stage
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: promotion-service
  namespace: stage
spec:
  selector:
    app: promotion-service
  ports:
  - port: 80
    targetPort: 8080

